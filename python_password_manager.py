pwd = input("What is the master password?")
while True:
    mode = input("a for add a new password, v for view a password")
    if mode == "a":
        pass
    elif mode == "v":
        pass
    else:
        print("Invalid Input")